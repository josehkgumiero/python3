# Import Statement
# Import some module
import sys

# Print the path which where the python is installed
#print("\nPrint the path which where the python is installed:")
print(sys.prefix)

# Print the path which where the c compiled files
#print("\nPrint the path which where the c compiled files:")
print(sys.exec_prefix)

# Print the direcories those where the modules will be searched
#print("\nPrint the directories those where the modules will be searched:")
print(sys.path)
